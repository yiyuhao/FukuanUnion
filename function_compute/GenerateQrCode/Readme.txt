
该文件是对函数计算二维码生成部分部署说明：
    本次二维码服务采用阿里云函数计算生成，位于阿里云杭州１，GenerateQrCode服务下。
    本目录下的py件，其中common是公用接口，其他别是一个计算服务，他们共用一个环境，但相互独立。

    部署时，若只改动服务中的代码，把代码覆盖阿里云上的对应服务打开在线编辑界面里面的代码即可。
    若改动了common代码，则需要重新打包部署，上传文件夹

    在打包部署时，需要把packages.zip中的三个包解压到当前文件夹下，再上传即可。

